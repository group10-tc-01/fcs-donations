apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fcs-donations-tls
  namespace: fcs-donations
spec:
  secretName: fcs-donations-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - fcs-donations.flaviojcf.com.br
