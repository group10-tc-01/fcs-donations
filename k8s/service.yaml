apiVersion: v1
kind: Service
metadata:
  name: fcs-donations-service
  namespace: fcs-donations
  labels:
    app.kubernetes.io/name: fcs-donations
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fcs-donations
  ports:
    - name: http
      port: 8080
      targetPort: http
